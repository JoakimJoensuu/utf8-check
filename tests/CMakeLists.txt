include(FetchContent)
enable_language(CXX)

set(CGREEN_WITH_UNIT_TESTS OFF CACHE BOOL "" FORCE)
set(CGREEN_WITH_STATIC_LIBRARY ON CACHE BOOL "" FORCE)
set(CGREEN_WITH_XML OFF CACHE BOOL "" FORCE)
set(CGREEN_WITH_LIBXML2 OFF CACHE BOOL "" FORCE)

set(GITREVISION "unknown" CACHE STRING "" FORCE)

FetchContent_Declare(
  cgreen
  GIT_REPOSITORY https://github.com/cgreen-devs/cgreen.git
  GIT_TAG 1.7.0
  GIT_SHALLOW TRUE)
set(CMAKE_EXPORT_COMPILE_COMMANDS OFF)
FetchContent_MakeAvailable(cgreen)
set(CMAKE_EXPORT_COMPILE_COMMANDS ON)

add_executable(test_utf8 utf8.c)
target_link_libraries(test_utf8 PRIVATE utf8c utf8c_warnings utf8c_sanitize cgreen_static)
target_include_directories(test_utf8 SYSTEM PRIVATE ${cgreen_SOURCE_DIR}/include)
add_test(NAME utf8 COMMAND test_utf8)
